<?php

$this->setup(array(
    'title' => 'Cancellation Policy page',
    'panel' => 'centered-panel large-sized',
    'back' => array(
        'Configuration' => $this->url('backend/config'),
    ),
    'links' => array(
        'Info page' => $this->url('backend/config/info'),
    ),
));

$this->headScript()->appendFile($this->basePath('js/tinymce/tinymce.min.js'));
$this->headScript()->appendFile($this->basePath('js/tinymce/tinymce.setup.js'));

?>

<div>
    <h1><?= $this->t('Cancellation Policy page') ?></h1>

    <div class="separator-small"></div>

    <p><?= $this->t('Which text should appear on the Cancellation Policy page?') ?></p>

    <?= $this->backendInfo('i18n') ?>

    <div class="separator-small"></div>

    <form method="post" action="<?= $this->url('backend/config/cancelpolicy') ?>">
        <div>
            <textarea name="cf-cancelpolicy" id="cf-cancelpolicy" class="wysiwyg-editor" style="height: 600px;">
                <?= $this->option('subject.cancelpolicy') ?>
            </textarea>
        </div>
    </form>
</div>